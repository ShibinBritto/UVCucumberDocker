const { forEach, toInteger } = require("lodash");
//simple code defined variable with datatype

const you="shibin";
  console.log(you);
let man= "shibin";
  let loos =[24, "welco", "AudioWorkletNode",45]
console.log(loos, man, you);
  function name(){
    let hirt=loos;
    if(hirt[0][3]==toInteger){
      console.log(loos[0],loos[3])
    }else{
      console.log(loos[1],loos[2])
    }
    for(let hirt of loos)
    console.log(hirt)
  }
  function male(){
    const insdie = man;
    for(let i=0;i<insdie.length;i++){
      let tin=(insdie[i]);
      console.log(tin)
    }
  }
  // return name(), male()

   


  
  
      function flipkart(){
          let count = 0;
          const uname = "aaeerrttookkllss";

          for (let i = 0; i < uname.length; i++) {
               count++;
          console.log(uname[i], count);
          if(uname[i] === uname){
              console.log("duplicate")
          }
  }
  let assam=['shibin','jagam',45,58,65];
          let seme =deal;
           for (let i = 0; i < assam.length; i++) {
               count++;
          console.log(assam[i], count);
      }
      let deal=["kilos","fastion","l","ty"]
      for (let i = 0; i < deal.length; i++) {
          count++;
          console.log(deal[i], count);
      }
   
      }
      return flipkart(), name(), male()
